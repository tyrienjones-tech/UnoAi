<!--
Format-spec for the validator (per DEC-026 + R8 + MS-006 Scope G):

Two parsers in scripts/validate.sh read this file:

(a) Counter parser (check 7). Reads lines starting with "Latest XXX:" where
    XXX ∈ {MS, DEC, RFI, INC} and the rest of the line contains an "XXX-NNN"
    identifier. **Do not reformat the four "Latest" counter lines.**

(b) Phase parser (check 10, README ↔ state sync). Reads the line starting
    with "Current:" inside the "## Phase" section, extracts the first
    "Phase X status" identifier, and compares against README's "## Status"
    first non-empty line. **Do not reformat the "Current:" line** — keep it
    starting with "Current: Phase X status..." so the parser can extract
    the identifier. Reformatting either side causes the pre-commit hook to
    hard-fail with a "README Status section out of sync" message.

The validator hard-fails if a parser cannot read these lines, because
line-format drift is exactly the failure mode mechanical enforcement should
catch. If you need to add commentary, put it on a separate line.

Auto-update timestamp: the "Updated:" line below should be refreshed on every
sign-out where state changes, per Procedure 9 (session lifecycle).
-->

# UnoAi — Current State

Updated: 2026-05-01 12:48 (auto-updated at session sign-out — Milo (Opus 4.7) fifth session, Stage A absolute-path scrub complete: 30 leak instances scrubbed across 7 files per Talos's substitution table + operator-confirmed Chat2U refinement; vault-tier names also scrubbed where they appeared in audit prose; single scrub commit at sign-out. Stage B = (a) accept historical leak per operator decision; Stage C bundled into MS-010 prep; Section 7 magic-string + Section 8 sign-in held for next session. Prior sign-out paragraph preserved below for context.) — Milo (Opus 4.7) fourth session, MS-009 Section 7 (doctrine alignment) work complete: audit-only verification check per (C) + R6 surface-don't-decide; doctrine read in-vault only at the operator-environment vault doctrine file (operator moved doctrine to higher-severity tier mid-session + provided clean .md paste replacing the .skill zip); 23 LESSONs + 24 Checklist items + 15 Trigger Conditions audited; Talos peer-review folded pre-commit (refinements to Findings 2/4/6 + cross-finding routing reorganisation); six findings surfaced + routed forward; no in-section filings per R6 + operator-decision-point (1) NO; fork-name references minimised to one anchor per operator instruction. Section 6 magic-string applied at sign-in via Option A chat-record authorisation (ninth DEC-032 exercise overall, seventh on SIGN_OFF.md surface). Section 7 SIGN_OFF entry filed `Status: in progress` awaiting operator chat sign-off; Section 8 (Phase 1 readiness gate) is next-session work per single-section rule. DEC-034 (deep-check methodology) + DEC-035 (DONE sign-off enforcement) + DEC-036 (Finding 2 re-route) + DEC-037 (Finding 6 build-time fail-closed primary) all candidates for filing at MS-009 close-out per operator decision today.) — Milo (Opus 4.7) third session, MS-009 Section 6 work complete: 6A cold-clone test PASS; 6B five-hook synthetic test cycle PASS across all 5 hooks (gitleaks, validator, Prettier, ESLint, cspell — each BLOCKED commit at the expected stage with proper diagnostics, then reverted clean); 6C cleanup PASS. **Pause-at-blocker invoked once** — npm install failed mid-6B.3 due to corrupted `@sigstore/sign` system file; operator authorised Node reinstall (Path A); 6B.3-6B.5 resumed clean post-reinstall. **INC-011 filed** documenting the npm corruption discovery (Latest INC INC-010 → INC-011). Two fold-ins applied per operator approval: Phase 1 re-discussion gate now in `## Phase` section; demotion entry to file in Agent Profiles post-commit. Section 6 SIGN_OFF entry filed `Status: in progress` awaiting operator chat sign-off; Section 7 (doctrine alignment) is next-session work per single-section rule.)

## Phase

Current: Phase 0b complete. Infrastructure phase in progress (MS-009). Phase 1 blocked on MS-009 close.
Last completed: MS-008 (DONE-008 signed 2026-04-28; second exercise of DEC-032 magic-string mechanism applied at MS-009 sign-in).
Next: MS-009 remaining sections (6, 7, 8 — single-session each per MS-009 prompt rules of engagement) → MS-010 → INSP-004 → MS-011 → Phase 1 (landing + payment + license).
**Phase 1 entry gate:** operator re-discussion required before first Phase 1 MS prompt is drafted (set 2026-04-30). Triggers after the last pre-Phase-1 MS closes (likely MS-011); pause and surface to operator for scope/direction re-discussion before drafting the Phase 1 first MS prompt.

## Active MS

MS-009 in progress (pre-Phase-1 deep check, 8 sections, section-by-section sign-off via `forms/SIGN_OFF.md`). Sections 0-6 signed off (Section 6 magic-string applied at this session sign-in 2026-05-01 10:59 via Option A chat-record authorisation — ninth DEC-032 exercise overall, seventh on SIGN_OFF.md surface). Section 7 (doctrine alignment) work complete and `Status: in progress` awaiting operator chat sign-off — audit-only per (C) + R6 surface-don't-decide; six findings surfaced + routed forward (DEC-036 + DEC-037 candidates for MS-009 close-out; MS-011 working-agreement candidates; Phase 1 re-discussion agenda items); Talos peer-review folded pre-commit; doctrine source for the audit was the operator-environment vault doctrine file per operator instruction (operator moved doctrine to higher-severity tier mid-session and provided clean .md replacing the .skill zip), **read-only with no content leaving the vault** per handling rule. Section 8 (Phase 1 readiness gate) pending in subsequent session per single-section-session rule.

## Counters (latest of each)

Latest MS:  MS-009
Latest DEC: DEC-033
Latest RFI: RFI-015
Latest INC: INC-011
Latest INSP: INSP-003

## MS chain status

- MS-001: complete (operator pre-authorised; no DONE filed — predates the DONE-required convention).
- MS-002: DONE (DONE-002, signed 2026-04-27).
- MS-003: DONE (DONE-003, signed 2026-04-27).
- MS-004: DONE (DONE-004, signed 2026-04-28).
- MS-005: DONE (DONE-005, signed 2026-04-28).
- MS-006: DONE (DONE-006, signed 2026-04-28).
- MS-007: DONE (DONE-007, signed 2026-04-28; sign-off recorded in DONE-007 entry per DEC-032 magic-string at MS-008 sign-in — first real exercise).
- MS-008: DONE (DONE-008, signed 2026-04-28; second exercise of DEC-032 magic-string mechanism applied at MS-009 sign-in).
- MS-009: in progress (pre-Phase-1 deep check, 8 sections, section-by-section sign-off via `forms/SIGN_OFF.md`; Sections 0-5 signed off 2026-04-28 — third through eighth exercises of DEC-032 magic-string mechanism; Section 6 (tooling end-to-end) work complete 2026-04-30 + signed off 2026-05-01 via Option A chat-record authorisation — ninth DEC-032 exercise overall, seventh on SIGN_OFF.md surface; Section 7 (doctrine alignment) work complete 2026-05-01, SIGN_OFF entry filed `Status: in progress` awaiting operator chat sign-off — audit-only per (C) + R6 + Talos peer-review folded pre-commit; Section 8 (Phase 1 readiness gate) pending in subsequent session per single-section-session rule).
- MS-010: pending; depends on MS-009 (security cluster from INSP-001 findings: HIGH-1 + MEDIUM-1..5 + INC-007 backfill + doctrine to docs/).
- INSP-002: pending; depends on MS-010 (verify MS-010 closed findings; cleaner friction surface for second inspection cycle).
- MS-011: pending; depends on INSP-002 (working agreements consolidation: 20+ entries → ~10-12 distinct rules).
- [first Phase 1 MS]: pending; depends on MS-011.

## Open RFIs

- RFI-009: TLD selection (`unoai.com` / `.app` / other) — pending operator decision, not blocking until pre-Phase-8.
- RFI-011: validator check 10 design tightness (working as designed but narrow scope; tighten, accept, or replace?) — pending Engineer scoping into MS-010 / MS-011, not blocking. Filed at MS-009 Section 3.
- RFI-013: Cloudflare Pages → Workers migration timing (stay on Pages for v1 vs migrate before v1 deploy; Engineer's lean: migrate before v1 deploy) — pending operator decision, blocks Phase 1 first deploy. Filed at INSP-002.
- RFI-014: email delivery mechanism for license tokens (LS confirmation email vs separate service like Resend/Postmark; Engineer has no lean) — pending operator decision, blocks Phase 1 webhook handler implementation. Filed at INSP-002.
- RFI-015: check 11 empty `done_mses` guard silently disables the check (LOW-1 in INSP-003 — fragility doesn't manifest in current state but would if `DONE.md` truncated or check 9 broke; Engineer's lean: bundle into MS-010 validator hardening cluster) — pending operator decision, not blocking. Filed at INSP-003 re-execution session 2026-04-30.

(RFI-010 closed 2026-04-28 via DEC-032 — magic-string-in-chat sign-off recording. RFI-012 closed 2026-04-28 via MS-009 Section 4 + INSP-002 MEDIUM-7 — banned moves PROJECT.md-as-canonical resolution.)

## Pending operator actions

- Cloudflare Pages account creation (blocks Phase 1 deployment). **Note:** RFI-013 (Pages → Workers migration timing; Engineer's lean: migrate before v1 deploy) may convert this to Cloudflare Workers account creation; await operator decision.
- Lemon Squeezy account + store + $9 placeholder product (blocks Phase 1 payment flow).
- Worker secret store ready for Ed25519 private key (blocks Phase 1 webhook handler).
- Domain registrar account + domain selection (deferred per RFI-009, pre-Phase-8 deadline).
- GitHub repo description update (currently still "Name is place holder" — verified via GitHub API at MS-009 Section 4, 2026-04-28).

## Last verified working state

2026-05-01 (Milo (Opus 4.7) fifth session — Stage A absolute-path scrub) — Resumed from HEAD = `9a86dbf` (2026-05-01 12:25 commit, MS-009 Section 7 close-out + INC-011-doctrine-handling-rule continuation). Push state verified clean at sign-in (`git ls-remote origin refs/heads/main` returned `9a86dbf` matching local main). Pre-session validator PASS. **Triggered by Talos repo-wide path-leak audit 2026-05-01 post-Section-7-commit:** operator-machine absolute paths leaking across 7 files (30 instances), exposing username + vault layout + sensitivity-tier scheme + OneDrive sync + Bash-on-Windows mapping + system-default nodejs install location. Operator confirmed all four decision points: Stage A proceed (this session); Stage B = (a) accept historical leak (force-push to public main is scrub-theatre once GitHub/clones/forks have indexed); Stage C bundled into MS-010 prep (validator check 12 candidate for absolute-path scan + working agreement; sibling check to Finding 2's broken-LESSON-NNN-ref candidate per Talos sub-note); Section 7 sign-off stands (audit substance unchanged by descriptor swaps). **Sign-in mechanical actions:** push state verified; **no DEC-032 magic-string this sign-in** — Section 7 magic-string `Section 7 signed off by operator on 2026-05-01.` held for application at Section 8 sign-in per operator decision (4); this scrub session is between Sections 7 and 8 and does not advance the MS chain. **Scrub execution per Talos's substitution table + operator-confirmed Chat2U refinement** (preserve `Chat2U` as a project-name reference where substantive content; descriptor-only where path is the leak). **30 leaks scrubbed across 7 files:** SIGN_OFF.md (3), SITE_LOG.md (5), state/current.md (4), INCIDENT.md (5), DONE.md (2), DECISION.md (1), METHOD_STATEMENT.md (12). Substitutions applied: vault doctrine path → "operator-environment vault doctrine file"; vault induction with absolute path → `Vault/INDUCTION.md` relative-shaped; UnoAi-repo absolute path → "the repo folder"; legacy `Chat2U` absolute path → "the legacy `Chat2U` folder" (name preserved); cold-clone temp absolute path → "the cold-clone working directory"; npm-cache debug log absolute path → "the npm-cache debug log named in the error message" / descriptor; operator Downloads source path → "operator-supplied source folder"; Agent Profiles absolute path → "Agent Profiles (Milo)"; Program Files / nodejs absolute path → `<nodejs-install>` placeholder + "system-default nodejs install location". Vault-tier names (`1- very sensitive`, `3 - Slight sensitive`) also scrubbed where they appeared in audit prose. **Final grep verification:** zero `C:\Users\Tyrien` and zero `C:\Program Files\nodejs` patterns remaining in repo `.md` content. **No semantic content changes** beyond leak-substring substitution; surrounding prose, claims, dates, decisions, historical-record content all preserved. **No new RFI / DEC / INC / CHANGE_ORDER filings; no counter advances; MS-009 sections substantively unchanged.** **Counter at sign-out (no advances):** Latest MS = MS-009; Latest DEC = DEC-033; Latest RFI = RFI-015; Latest INC = INC-011; Latest INSP = INSP-003. Validator PASS at sign-out. **Pause-at-blocker discipline:** zero triggering moments this session. All substitutions executed within plan; the sign-in entry's own substitution-table section was rewritten in placeholder form pre-commit so the table itself doesn't introduce new literal-path leaks (one own-sweep correction caught and self-applied before commit). **Pattern worth memorialising:** Talos peer-review scope can productively cover repo-wide hygiene patterns that section-bounded audits miss — five prior inspections (INSP-001/002/003 + the doctrine-handling-rule sweep + the fork-name-reference minimisation sweep) all missed this class of leak in their explicit scopes; Talos's repo-wide audit caught it. MS-011 working-agreement candidate.

2026-05-01 (Milo (Opus 4.7) fourth session — MS-009 Section 7: doctrine alignment) — Resumed from HEAD = `0684441` (2026-04-30 23:53 commit, Section 6 close-out + INC-011). Push state verified clean at sign-in (`git ls-remote origin refs/heads/main` returned `0684441` matching local main). Pre-session validator PASS (HEAD `0684441`, working tree clean). **Sign-in mechanical actions:** Section 6 sign-off DEC-032 magic-string applied to `forms/SIGN_OFF.md` Section 6 entry — `Section 6 signed off by operator on 2026-05-01.` written verbatim; Status flipped `in progress → signed off`; Date signed flipped `pending → 2026-05-01`. **Operator chat-record authorisation (Option A handling)** documented in a new Builder note within Section 6's Notes section — operator instruction *"apply current time and date... sign my name"* on 2026-05-01 00:13 WAST authorised Builder to compose the magic-string with today's WAST date (deviation from strict DEC-032 *"operator types"* pattern; magic-string itself stayed format-rigid for validator check 11 conformance). Ninth DEC-032 exercise overall; seventh on SIGN_OFF.md surface. MS chain bumped (Sections 0-6 signed off; Section 7 in progress this session); `Active MS` line revised. **Section 7 audit (audit-only per (C) + R6 surface-don't-decide; doctrine handling rule held throughout):** doctrine read in-vault only at the operator-environment vault doctrine file — operator moved doctrine to higher-severity tier mid-session and provided clean .md paste replacing the .skill zip (the .skill turned out to be a ZIP archive containing `eco-agentic-doctrine/SKILL.md`, surfaced via pause-at-blocker; operator obtained clean paste from a Claude.ai session with the canonical skill installed). 23 LESSONs walked: 13 ALIGNED + 4 ALIGNED-WITH-NUANCE + 4 PARTIAL + 2 NOT-APPLICABLE. 24 ECO Design Checklist items walked: 14 ALIGNED + 4 PARTIAL + 6 NOT-APPLICABLE + 4 OPERATOR-SIDE. 15 Trigger Conditions walked: all 15 have a UnoAi procedural surface that fires. **Six findings surfaced + routed forward:** (1) Strong overall alignment; (2) INSP-001 MEDIUM-5 routing re-evaluation per fork-clean architecture + vault-references-not-duplicates rule (load-bearing finding; original *"doctrine to docs/"* routing conflicts with three architectural surfaces; recommended re-route is harden engineer-session-start.md cross-environment reference + add UnoAi-side LESSON-to-surface index in PROJECT.md + drift discipline (anchor index to vault SHA + validator check 12 candidate for broken `LESSON-NNN` refs + fork-inheritance question for the planned private R&D fork + validator-coverage gap closure); DEC-036 candidate routing to MS-009 close-out per operator decision today); (3) Builder reasoning observability partial gap (LESSON-019; MS-011 candidate); (4) Builder behavioural instructions distributed (revised per Talos peer-review to anchored-index pattern over consolidation; MS-011 candidate); (5) Approval-fatigue risk in Phase 1+ MSes (LESSON-015; Phase 1 re-discussion agenda candidate); (6) Phase 8 deploy-verification step alignment (revised per Talos peer-review to build-time fail-closed PRIMARY + deploy-time confirmation BACKUP; DEC-037 candidate routing to MS-009 close-out per operator decision today). **Talos peer-review folded pre-commit** (substantive refinements to Findings 2/4/6 + cross-finding routing reorganisation + vault-access spot-check requests deferred per operator-decision-point (4)). **Operator decisions resolved 2026-05-01 (post-Talos confirm):** (1) NO on in-section DEC filing — DEC-036 + DEC-037 land at MS-009 close-out (Section 8) or MS-010 prep, not in Section 7; (2) moot; (3) fork-inheritance for the planned private R&D fork parked with Talos's substantive read captured for the future DEC-drafting gate (read: *the fork does not inherit the LESSON-to-surface index — pointers to inaccessible LESSONs would be a contract the fork cannot fulfil; strip during fork generation or gate behind a build-time flag*); (4) defer on vault access for Talos LESSON spot-checks; (5) commit message unchanged (`MS-009 Section 7: doctrine alignment (audit-only, fork-clean routing)`); (6) Talos refinements stand as folded in. **Fork-name references minimised** per operator instruction 2026-05-01: one anchor at Finding 2 sub-point (c) (the open architectural question literally is about her); descriptor language (*"the private R&D fork"* / *"the planned fork"* / *"the fork"*) used elsewhere across SIGN_OFF.md + SITE_LOG.md. **Pause-at-blocker invoked once** (mid-section): doctrine `.skill` file proved to be a ZIP archive (binary on Read tool); surfaced to operator with three options; operator chose to obtain clean .md paste from external Claude.ai session and provided at higher-severity tier; doctrine read resumed clean post-paste. **Counter at sign-out (no advances this session):** Latest MS = MS-009; Latest DEC = DEC-033; Latest RFI = RFI-015; Latest INC = INC-011; Latest INSP = INSP-003. **Section 7 SIGN_OFF.md entry filed `Status: in progress`** (~155 lines including three-checklist sub-scope sections + six findings + Notes documenting handling rule, vault location, doctrine source change, Talos peer-review contributions, operator decisions resolved); awaiting operator chat sign-off (Section 8 unlocks next). Validator PASS at sign-out. **Talos peer-review pattern worth preserving** (handover note candidate for MS-011): peer-review-before-commit on audit-shaped sections produces tighter findings than Builder-only drafting.

2026-04-30 (Milo (Opus 4.7) third session — MS-009 Section 6: tooling end-to-end) — Resumed from HEAD = `61c7cba` (12:43 commit, INSP-003 + bug-fix re-execution close-out). Pre-session validator PASS (HEAD `61c7cba`, working tree clean). **Sign-in mechanical actions:** push state verified; no DEC-032 magic-string this sign-in (Section 5 already applied 2026-04-30 12:23); **Phase 1 re-discussion gate landed** in `## Phase` section per operator approval (one-line note replacing the misplaced Claude Code auto-memory entry — that auto-memory file remains on disk per "just skip" but is no longer canonical); MS chain bumped (Section 6 in progress); `Active MS` line revised. **6A — Cold-clone test:** `git clone https://github.com/tyrienjones-tech/UnoAi.git /tmp/unoai-cold-clone` resolved cleanly (bash-on-Windows mapped /tmp transparently to the cold-clone working directory); pre-activation `core.hooksPath` empty (confirms per-clone opt-in per CONTRIBUTING.md); post-activation `git config core.hooksPath .githooks` set; `bash scripts/validate.sh` from cold clone returned `VALIDATOR: PASS` exit 0; HEAD verified `61c7cba` matching origin and live. **6B — Five-hook synthetic test cycle (in cold clone):** all five hooks confirmed BLOCKING correctly with clean diagnostics — 6B.1 gitleaks (synthetic Anthropic API key, RuleID `anthropic-api-key-strict`, exit 1); 6B.2 validator (counter mismatch INC-099 vs INC-010, specific FAIL message, exit 1); 6B.3 Prettier (mis-formatted JSON, formatting flag, exit 1) — *retried post-Node-reinstall after first attempt surfaced npm corruption*; 6B.4 ESLint (camelCase type alias `myBadType` violating @typescript-eslint/naming-convention, exit 1); 6B.5 cspell (typo `absolutley` + gibberish `frqxblargx` outside override scope, "Unknown word" output, exit 1). Each test reverted clean (file unstaged + deleted; for validator test, `git checkout state/current.md`). **Pause-at-blocker invoked once during 6B.3 first attempt:** `npm install` failed in both cold clone AND live working tree with `SyntaxError: Unexpected token 'return'` from `<nodejs-install>\node_modules\npm\node_modules\@sigstore\sign\dist\witness\tsa\client.js:40` (stray `$` token, system-level file corruption in the system-default nodejs install location). Surfaced to operator; investigated via npm-cache debug log named in error message; root cause identified as corrupted npm-internal file. Live UnoAi commits had been working all day because they used pre-existing `node_modules/` from a prior successful install (don't trigger the broken `@sigstore/sign` code path). **INC-011 filed** during the Node-reinstall window documenting the npm corruption discovery (Latest INC INC-010 → INC-011). Operator authorised Path A (Node reinstall from nodejs.org); post-reinstall `npm install` retry succeeded (343 packages, exit 0; 3 low-severity vulns flagged for audit info — out of scope this session); 6B.3-6B.5 then completed with clean diagnostics. **6C — Cleanup:** `rm -rf /tmp/unoai-cold-clone` executed; cold-clone dir absent; live working tree clean of cold-clone artefacts (only intended four dirty files this session: `.cspell.json`, `forms/INCIDENT.md`, `forms/SITE_LOG.md`, `state/current.md` — INC-011 + sign-in/sign-out + sigstore override addition + counter bump); validator PASS. **Findings (logged in SIGN_OFF Section 6 entry):** Finding 1 npm corruption (resolved this session via INC-011); Finding 2 CONTRIBUTING.md should document `npm install` as part of per-clone setup (routing: MS-010 documentation cluster or small standalone amendment — not actioned this session per scope guards); Finding 3 npm audit reports 3 low-severity vulns (informational, not actioned this session). **Cspell additions:** `sigstore` added to forms/state override (audit-trail scope only — not main words; scope-narrow because UnoAi doesn't use sigstore directly, only mentioned in INC-011 context). **One own-prose rephrase:** `load-bearing` → `load-bearing` in sign-in entry per `cspell_dictionary_vs_rephrase` rule. **Counter at sign-out:** Latest MS = MS-009; Latest DEC = DEC-033; Latest RFI = RFI-015; Latest INC = INC-011 (bumped this session); Latest INSP = INSP-003. **Section 6 SIGN_OFF.md entry filed `Status: in progress`** with three sub-scope checklist sections (6A four-checklist, 6B five-checklist, 6C three-checklist) + three findings + comprehensive notes; awaiting operator chat sign-off (Section 7 unlocks next). Validator PASS at sign-out (HEAD will be the close-out commit; counters verified; check_sequential clean across DEC/RFI/INC/MS; check 11 PASS — DONE-002..008 all carry valid DEC-032 magic-strings). **Validates Section 6's design:** cold-clone test surfaced an environmental issue (npm corruption) that three prior inspections (INSP-001/002/003) all missed because they reviewed the live tree — confirming the unique discovery value of fresh-environment testing. **Phase 1 re-discussion gate visible** at `state/current.md` `## Phase` section starting this commit; will be read at every future session sign-in by every agent.

2026-04-30 (Milo (Opus 4.7) second session — INSP-003 + bug-fix re-execution) — Resumed from HEAD = `423465a` (INC-010 close-out commit). Push state verified clean at sign-in (`git ls-remote origin refs/heads/main` returned `423465a` matching local main). Pre-session validator PASS (HEAD `423465a`, working tree clean post-INC-010-commit). **Sign-in mechanical actions:** Section 5 sign-off DEC-032 magic-string applied to `forms/SIGN_OFF.md` per prior-Engineer Q3 verbatim confirmation `Section 5 signed off by operator on 2026-04-28.` (eighth DEC-032 exercise overall, sixth on SIGN_OFF.md surface — first legitimate Section 5 application since prior 2026-04-29 WIP was discarded by reset). MS chain status bumped from "Sections 0-4 signed off / Section 5 in progress" to "Sections 0-5 signed off". Active MS line revised to reflect this session's INSP-003 + bug-fix scope. **Scope A (`asdfqwerty` cleanup):** removed from `.cspell.json` main `words` array; added per-path override `["forms/**/*.md", "state/current.md"]` per operator decision (option (iii) glob form). Synthetic violation test on temp `.md` outside override scope: cspell FLAGGED both occurrences with "Unknown word (asdfqwerty)", exit 1 — confirms commit would BLOCK. Inside-scope cspell PASS confirmed on `forms/INCIDENT.md` (which contains INC-010 references to asdfqwerty). Temp file deleted. **Scope B (validator check 11 awk polarity fix):** `scripts/validate.sh` updated; check 11 awk regex changed from negative-pattern (`!~ /^[Pp]ending\.?[[:space:]]*$/`) to positive-pattern (`~ /^DONE-[0-9]+ signed off by operator on [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]\.[[:space:]]*$/`); header comment block + check 11 inline comment updated to reference INSP-003 MEDIUM-1 polarity fix. 4-case synthetic test cycle on DONE-008 `Operator sign-off:` field: (i) empty content → `VALIDATOR: FAIL` exit 1 with `MS-009 cannot proceed — depends on MS-008 whose DONE entry has 'Operator sign-off: pending' (not signed)` ✓; (ii) `Pending review by ops.` prose → FAIL exit 1 same message ✓; (iii) template placeholder `[pending / DONE-NNN signed off by operator on YYYY-MM-DD]` → FAIL exit 1 same message ✓; (iv) restored real magic-string → `VALIDATOR: PASS` exit 0 ✓. Final state restored to real magic-string; DONE.md unchanged from committed state per `git diff --stat` empty. **Scope C (INSP-003 filing):** ~150-line verbatim body from prior-Engineer appended to `forms/INSPECTION.md` at H2 per form convention; `[filed at re-execution date]` placeholder set to `2026-04-30`; MEDIUM-2 RESOLUTION pre-applied per Option A (preserves "bug found → fix attempt abandoned → re-execution closed it" audit trail); INC-010 cross-reference pre-applied per updated content (procedural break, not Engineer planning failure). Four en-GB prose-choice normalisations applied at write-time per project rule (memory feedback `cspell_dictionary_vs_rephrase`, operator-validated 2026-04-28): three US-spelling adjectives in INSP-003 (Scope line + Summary + Open Questions) and one in RFI-015 (Context); for one pair where both US and UK forms are flagged by cspell, substituted a dictionary-clean adjective conveying the same meaning rather than adding either form to the dictionary; one unintended over-edit on a clean original word was caught and self-reverted before commit. **Scope D (RFI-015 filing):** verbatim body from prior-Engineer appended to `forms/RFI.md` at H3 per form convention; Date kept as `2026-04-29` per operator decision (drafting date, not filing date); one en-GB normalisation in the Context section (US-spelling noun normalised to UK form). **state/current.md updates at sign-out:** `Latest INSP` INSP-002 → INSP-003 (counter bumped); `Latest RFI` RFI-014 → RFI-015 (counter bumped); `Open RFIs` list adds RFI-015 entry with Engineer's lean (bundle into MS-010 validator hardening cluster); this paragraph; `Updated:` timestamp refreshed. Validator PASS at sign-out (HEAD will be the close-out commit; counters: Latest MS = MS-009, Latest DEC = DEC-033, Latest RFI = RFI-015, Latest INC = INC-010, Latest INSP = INSP-003; check_sequential clean across DEC/RFI/INC/MS; check 11 PASS — DONE-002..008 all carry valid DEC-032 magic-strings). **Pause-at-blocker discipline:** zero triggering moments this session. All four scopes executed within plan; cspell rephrase decisions handled per established `dictionary_vs_rephrase` rule; one own-error caught and self-reverted (an over-edit on a clean original word, fully reverted). **Counter at sign-out:** Latest MS = MS-009; Latest DEC = DEC-033; Latest RFI = RFI-015; Latest INC = INC-010; Latest INSP = INSP-003.

2026-04-30 (Milo (Opus 4.7) first session — procedural cleanup) — Working-tree reset to HEAD = `4feef75` per operator authorisation following overnight dangling-state audit (operator instruction "1 read only audit, figure out what left before finish" → audit findings → operator "I agree and approve" on the proposed sequence). Six WIP files inherited from prior-agent 2026-04-29 10:11 session discarded: `forms/SITE_LOG.md` (orphan sign-in entry, no matching sign-out), `forms/SIGN_OFF.md` (Section 5 magic-string applied to working tree), `forms/INSPECTION.md` (INSP-003 entry filed at H2), `scripts/validate.sh` (check 11 awk polarity fix from negative-pattern → positive-pattern), `state/current.md` (sign-in updates), `.cspell.json` (asdfqwerty removed from main words array; retained in forms/state override). No retroactive 2026-04-29 sign-out filed — reset removed the sign-in entry itself; INC-010 carries the documentation. Validator PASS at sign-in (HEAD `4feef75`, working tree clean post-reset). **INC-010** filed as first work action this session per Procedure 9 missing-sign-out clause; documents the procedural break (~24h dangling state, six dirty files, no commit, no sign-out, silent INSP counter drift) and three working-agreement candidates (sign-out/commit/push joint, validator stale-sign-in check, INSP counter coverage). No DEC-032 magic-string applied this session; Section 5 chat sign-off (operator 2026-04-28) deferred for re-application at next legitimate Section 5 sign-off cycle per operator's "back up the plan, don't jump halfway" directive. Counters at sign-in: Latest MS = MS-009, Latest DEC = DEC-033, Latest RFI = RFI-014, Latest INC = INC-010 (bumped this session), Latest INSP = INSP-002 (INSP-003 not re-filed this session). MS chain status unchanged from 4feef75 (MS-009 Sections 0-4 signed off, Section 5 work complete and `Status: in progress` awaiting operator chat sign-off, Sections 6-8 pending). **First sign-in under the `Role: Milo (Opus 4.7)` convention** per role-table update of 2026-04-30; replaces prior `Role: Builder` value used in post-Procedure-9 entries. Strict heading regex unchanged.

2026-04-29 (MS-009 Section 5 — Builder, single-section session) — Push state verified clean at sign-in (`git ls-remote origin refs/heads/main` returned `cfc6071` matching local main HEAD). Section 4 sign-off magic-string applied per DEC-032 seventh exercise / fifth on SIGN_OFF.md surface (`Section 4 signed off by operator on 2026-04-28.`). Pre-section validator PASS at HEAD `cfc6071` (289 lines, 10 checks + deferred 11th in header comments). **Sub-scope 5A — per-form integrity audit** clean across all nine forms (DECISION.md DEC-001..033 sequential with supersede chains resolving; RFI.md RFI-001..014 sequential with status / closure citations correct; INCIDENT.md INC-001..009 sequential with root-cause / resolution / lesson sections; METHOD_STATEMENT.md MS-001..009 sequential; DONE.md DONE-002..008 sequential with the MS-001 carve-out documented at PROCEDURES.md:67 footnote; SITE_LOG.md sign-in/out pairing PASS via validator check 8; CHANGE_ORDER.md entries empty with template/example CO-002 numbering deferred to MS-010; SIGN_OFF.md Sections 0-4 signed-off; INSPECTION.md INSP-001 + INSP-002 properly structured with cross-references resolving). One audit-not-fix finding flagged for MS-010/MS-011 routing: MS template + MS-001..MS-004 entries lack explicit `**Depends on:** / **Blocks:**` field lines despite INC-006 H2b/H2c claims (validator's check 9 + new check 11 only validate MS-005 onwards as a result). **Sub-scope 5B — retroactive DONE sign-off cleanup** applied: five operator-supplied verbatim magic-strings appended to DONE-002..006 `Operator sign-off:` fields per Engineer-supplied content (DONE-002 + DONE-003 dated 2026-04-27; DONE-004 + DONE-005 + DONE-006 dated 2026-04-28). All seven existing DONE entries (DONE-002..008) now carry populated sign-off fields. First application of DEC-032 to historical entries (prior six exercises were forward-looking from DONE-007 onwards). **Sub-scope 5C — validator check 11 shipped + synthetic-tested**: `scripts/validate.sh` updated from 289 lines / 10 checks to 324 lines / 11 checks (+35 lines: ~13 lines header comment block update, ~22 lines new implementation code). Check 11 walks the same `Depends on: MS-NNN` lines as check 9 and verifies each target's DONE entry's `Operator sign-off:` field is non-pending; FAIL message names the source MS and the unsigned dependency and references DEC-032 as the resolution path. Synthetic test cycle (working agreement #8): pre-toggle `VALIDATOR: PASS` exit 0; post-toggle (DONE-008 sign-off field reverted to `pending`) `VALIDATOR: FAIL` exit 1 with the message `DONE sign-off: MS-009 cannot proceed — depends on MS-008 whose DONE entry has 'Operator sign-off: pending' (not signed). DEC-032 magic-string must be applied at next session sign-in.`; post-revert `VALIDATOR: PASS` exit 0. Check 11 fires on the expected violation, names both source and target, and clears on revert. `GLOSSARY.md:161` `**validator**` definition updated to "11 checks" with DEC-032 enforcement called out and MS-009 Section 5 ship-event noted; no other current-state numeric check-count references found. Section 5 SIGN_OFF.md entry filed `Status: in progress` with three sub-scope checklist sections + two findings + five notes; awaiting operator chat sign-off (Section 6 unlocks next). Validator PASS at sign-out (HEAD will be the close-out commit; counters: Latest MS = MS-009, Latest DEC = DEC-033, Latest RFI = RFI-014, Latest INC = INC-009, Latest INSP = INSP-002; check_sequential clean across DEC/RFI/INC/MS; new check 11 PASS on the seven Depends on lines from MS-005..MS-009; check 10 PASS — README's `## Status` line still extracts `Phase 0b complete` matching state's `Current: Phase 0b complete.`). No new numbered entries (DEC / RFI / INC / MS) filed this section.

2026-04-28 (MS-009 Section 4 — Builder, single-section session) — Push state verified clean at sign-in (`git ls-remote origin refs/heads/main` returned `bb88533` matching local main). Pre-session validator PASS at HEAD `bb88533` (289 lines unchanged). **State integrity audit (Section 4 step 4) clean** across the eight sub-areas: Phase line accurate; Active MS line accurate (with one informational note about the "this session's sign-in" phrase re-binding across sessions, see SIGN_OFF Section 4 Finding 4); all five counters match form tail entries (verified via grep on `forms/METHOD_STATEMENT.md`, `forms/DECISION.md`, `forms/RFI.md`, `forms/INCIDENT.md`, `forms/INSPECTION.md`); MS chain status accurate (MS-001..MS-008 done with DONE entries DONE-002 through DONE-008, MS-001 carve-out documented in PROCEDURES.md:67 footnote); Open RFIs list matches reality (4 entries post step-5c: RFI-009, RFI-011, RFI-013, RFI-014); pending operator actions audited (5 entries post-cleanup, 1 obsolete entry removed); working agreements list complete 1-20 sequentially numbered; banned moves now match PROJECT.md verbatim. **RFI-012 closed** (`forms/RFI.md` `Operator decision:` field updated to `ANSWERED 2026-04-28 via MS-009 Section 4 + INSP-002 MEDIUM-7`; `state/current.md` `Open RFIs` list dropped RFI-012 and added closure-line note). **PROJECT.md banned-moves bullet list extended from 9 to 10 items** (added: `` `Co-Authored-By Claude` footers on commits — operator-attributed commits only; AI-assistance attribution is a project policy decision filed as DEC if/when revisited.``). **state/current.md banned-moves section replaced with 10 items verbatim from PROJECT.md** (mirror header `(mirror of PROJECT.md, restated for session-start visibility)` is now accurate). **Pending operator actions list pruned**: README self-hosting wording amendment removed as obsolete (operator commit `bf2d661` shipped the wording per DONE-005 close-out evidence; MS-005 closed the heading-marker artefact); Cloudflare Pages line annotated with RFI-013 dependency conditional; GitHub repo description line annotated with API-verification context (`"description": "Name is place holder"` confirmed at section). Section 4 SIGN_OFF.md entry filed `Status: in progress` with four findings + five notes; awaiting operator chat sign-off (Section 5 unlocks next). Validator PASS at sign-out (counters: Latest MS = MS-009, Latest DEC = DEC-033, Latest RFI = RFI-014, Latest INC = INC-009, Latest INSP = INSP-002; check_sequential clean across DEC/RFI/INC/MS; check 10 PASS — README's `## Status` line still extracts `Phase 0b complete` matching state's `Current: Phase 0b complete.`). No new numbered entries (DEC / RFI / INC / MS) filed this session beyond the SIGN_OFF Section 4 entry.

2026-04-28 (INSP-002 filing session — Builder) — Section 3 sign-off magic-string applied at sign-in (DEC-032 sixth exercise, fourth on SIGN_OFF.md surface). Push state verified clean (`git ls-remote origin refs/heads/main` returned `16a70c9` matching local main). Pre-session and mid-session validators PASS at HEAD `16a70c9` (289 lines unchanged). **INC-007** filed (operator personal email caught in INSP-001 by gitleaks — long-deferred from Inspector handover note 3 / MS-009 Section 5; resolved in same commit per operator's Option-A direction after Builder pause-at-blocker on the INC-008-without-INC-007 sequence gap). **INC-008** filed (Engineer verification miss in INSP-002 action plan PDF v1.0 — same family as working agreements #19 + #20; named MS-011 working-agreement candidate #21 by Engineer for verify-before-asserting principle). **INC-009** filed (Engineer heading-level transcription drift across the INSP-002 session prompt — H2 used for all five new entries when INCIDENT.md and RFI.md convention is H3; Builder caught at validator FAIL pre-commit, operator authorised mechanical H2→H3 conversion for the four INC/RFI entries while INSP-002 retained H2 per INSPECTION.md convention; named MS-011 working-agreement candidate for "verify form-specific conventions before drafting numbered entries"; sub-case of the broader "verify before asserting" principle). **INSP-002** filed (external review consolidation — security + supply chain + architecture + procedural drift; 0 CRITICAL, 0 new HIGH, 8 MEDIUM, 5 LOW, 4 INFO; supplements but does not supersede INSP-001; recommends INSP-003 after MS-010). **RFI-013** filed (Cloudflare Pages → Workers migration timing; Engineer's lean: migrate before v1 deploy; blocks Phase 1 first deploy). **RFI-014** filed (email delivery mechanism for license tokens; Engineer has no lean; blocks Phase 1 webhook handler). MS-009 Sections 4-8 still pending in subsequent sessions; this session deliberately ended at the INSP-002 + INC-007 + INC-008 + INC-009 + RFI-013 + RFI-014 boundary per the prompt's rules of engagement. Validator PASS at sign-out (counters reflect Latest INC = INC-009, Latest RFI = RFI-014, Latest INSP = INSP-002; check_sequential clean across DEC/RFI/INC/MS).

2026-04-28 (MS-009 second session — Section 3) — Section 2 sign-off magic-string applied at sign-in (DEC-032 fifth exercise, third on SIGN_OFF.md surface); working agreements #17-#20 added to running list (cross-environment paths; explicit-approval; path-verification; new-agent-role first-session friction — all four traceable to INSP-001 first-inspection lessons). MS chain status updated for post-INSP-001 sequence (MS-009 → MS-010 (security cluster from INSP-001) → INSP-002 → MS-011 (working-agreements consolidation) → first Phase 1 MS). Section 3 (documentation accuracy) work complete: `PROCEDURES.md:67` DONE-001-onwards italicised footnote added explaining the MS-001 carve-out (closes Section 2 carry-forward Finding #1); `README.md:85` Status section updated to current-state phrasing (`Phase 0b complete. Infrastructure phase in progress (MS-009). Phase 1 begins after MS-009 → MS-010 → INSP-002 → MS-011.` — closes Section 2 carry-forward Finding #2 + INSP-001 LOW-3); `README.md:63` `.githooks/` parenthetical updated to include `cspell` as 5th hook step; `README.md:52-79` file map updated to add `forms/SIGN_OFF.md`, `forms/INSPECTION.md`, `.cspell.json` (tree connectors adjusted; INSPECTION.md becomes new last entry under `forms/`). RFI-011 filed (validator check 10 design tightness — Engineer to scope into MS-010 / MS-011); RFI-012 filed (banned moves divergence between PROJECT.md and state/current.md — Engineer to direct reconciliation; state/current.md banned moves header NOT modified in Section 3 per the section's audit-not-fix posture). All current-state stale-reference scans clean (`Chat2U`, `$39`, count drift across procedure / validator-check / hook-step / "X rules" — zero current-state hits); all seven MS-005 / MS-006-named "For agents reading the code" subsections present in PROJECT.md (plus 2 bonus). Validator PASS at sign-out (HEAD will be the close-out commit; validator at 289 lines unchanged — no validator code change in this section). Section 3 SIGN_OFF.md entry filed `Status: in progress`, awaiting operator chat sign-off; Section 4 (state integrity) is next session's work, not this session's.

2026-04-28 (INSP-001, Inspector audit) — pre-Phase-1 readiness audit filed at `forms/INSPECTION.md` (new form, created with header by Inspector this session). Validator PASS at session start (HEAD `586d6d0`) and PASS at session end (post-INSPECTION.md creation; new form is invisible to validator's check 1-10 surface). gitleaks clean across 508 KB of working-tree content. Audit posture: 0 CRITICAL, 1 HIGH (`CONTRIBUTING.md` security-contact placeholder), 5 MEDIUM (hook activation opt-in, hook/validator tamper-detection, --no-verify bypass, npm supply-chain monitoring, doctrine in-repo placement), 5 LOW (.gitleaks.toml self-reference, two operator email identities in commit history, README phase-content staleness already on Section 3 list, no CI, playwright install). All findings include recommendations; Engineer drafts MSes for action. Recommended next inspection: INSP-002 post-Phase-1 product code. **Procedural deviation noted in INSP-001 itself:** Inspector wrote INSP-001 counter and this last-verified-state line to working tree but did not commit `state/current.md` (left for Builder to fold in at MS-009 Section 2 close-out, because Builder's MS-009 Section 2 in-progress edits to the MS chain status section made selective staging non-trivial without destructive operations Inspector wasn't authorised to use).

2026-04-28 (MS-009 first session) — `forms/SIGN_OFF.md` created at repo root in `forms/` directory with 8-section scaffold (Section 0 + Sections 1-8); each entry has Status / Date filed / Date signed / Operator sign-off fields plus Checklist results / Findings / Notes subsections. `PROCEDURES.md` updated with one-paragraph note about SIGN_OFF.md (placement: post-Procedure-9, pre-Summary-table); procedure count stays at nine. **Section 0** (SIGN_OFF.md creation + PROCEDURES.md note) work complete, status `in progress`, awaiting operator chat sign-off. **Section 1** (spelling & grammar) work complete: cspell baseline + post-edit runs both PASS (18 files, 0 issues); 6 en-GB conversions applied across 4 files (`CONTEXT.md` ×2, `PROJECT.md` ×1, `prompts/engineer-session-start.md` ×2, `prompts/engineer-prompt-checklist.md` ×1) — all in the form `optimized → optimised`, `centralized → centralised`, `summarize → summarise`, `authorize → authorise`. Manual grammar pass found zero meaning-changing errors across the 8 in-scope prose-heavy files. Out-of-scope files (`forms/*`, `state/current.md`) still contain occasional US spellings in historical entries; deliberately left per Section 1 scope boundary. No new tooling. No validator changes. No new working agreements. Pre-commit hook chain unchanged (5 steps).

2026-04-28 (MS-008) — `GLOSSARY.md` landed at repo root with ~50 alphabetical entries (operator-supplied content + Builder-drafted entries for Vitest / synthetic test / validator size budget per Open-Item-1 RFI resolution). `cspell` v10.0.0 installed as devDependency, `.cspell.json` configured (en-GB + 50+ project terms in dictionary), wired into pre-commit hook as 5th step (gitleaks → validator → Prettier → ESLint → cspell). First-run cleanup: 159 issues across 15 files initially → 0 issues after dictionary additions + bulk US→UK fixes (per Scope B5 bulk-conversion authority). Synthetic violation test (B6) confirmed cspell BLOCKS commits with definitively-misspelled content (`asdfqwerty`); revert + clean run passes. Note: `teh` is in cspell's default dictionary (treated as valid in some company-name dictionary), so the synthetic test used a less-ambiguous typo. Procedure 3 "Proof division" updated for doc-only-MS work-type-conditional operator-capture (per Scope C). DEC-033 filed. RFI-010 closure (from MS-007) carried over. Working agreements #14 refined to three-tier (git ls-remote / API endpoint / never-HTML), #15 added (glossary scope must be grep-evidence-based), #16 added (approval messages need actual content not placeholders).

2026-04-28 (MS-007) — Agent onboarding infrastructure landed. `prompts/engineer-session-start.md` (operator-pasted prompt for fresh Engineer sessions, references Anthropic-environment doctrine paths) + `prompts/engineer-prompt-checklist.md` (mechanical fix for working-agreement #5/#10/#11 drift, scope-noted to prompt-writing only) + `CONTEXT.md` (agent-onboarding-focused, distinct from PROJECT.md and README.md) + cross-references to `prompts/engineer-session-start.md` from README and PROJECT.md. PROCEDURES.md Procedure 1 gained Engineer-role addendum about reading the prompt-checklist before drafting prompts. RFI-010 closed via DEC-032 (DONE sign-off recording = magic-string-in-chat copied verbatim by Builder at next sign-in). DONE.md template annotated with the magic-string format. Validator script header documents the deferred 11th check (DONE sign-off enforcement) — implementation deferred to MS-009+ until DONE-002..006 retroactive cleanup completes. No validator code changes; no synthetic tests (working agreement #8 applies only when tooling is added).

2026-04-28 (MS-006) — Validator gained 10th check (README ↔ state sync). Synthetic test verified: state's "Current: Phase 99 in progress" is correctly caught against README's "Phase 0b complete". Final clean run PASS. Validator is 277 lines, 10 checks. Pre-commit hook chain extended to 4 steps: gitleaks → validator → Prettier (staged) → ESLint (staged); all four enforce on every commit. ESLint naming-convention rule landed with 0 false positives on existing scaffold. PROJECT.md "For agents reading the code" gained 4 new subsections (Code directory structure, Test layout, Errors and logging, Dependency policy). `.env.example` placeholder file landed with gitleaks rule capture-group fix so placeholder values pass cleanly. Skeleton dirs created under `src/lib/` for Phase 1+ work. DEC-028..031 filed (directory structure, test layout, errors+logging, dependency policy).

2026-04-28 (MS-005) — Validator gained 9th check (MS chain dependency). Both branches verified via synthetic tests: target-missing ("MS-099 does not exist") and target-undone ("MS-001 exists but is not yet DONE"). PROJECT.md "For agents reading the code" section added (DEC-027). README markdown fixes landed (`## Self-hosting` heading restored, Status line current). Validator was 237 lines at MS-005 close, 9 checks; the 150-line guidance from MS-004 is retired per working agreement #10. INC-006 captured the MS-004 enforcement-gap pattern; RFI-010 filed for the DONE sign-off recording mechanism (deferred to MS-007).

2026-04-28 (MS-004) — Session lifecycle live. Validator (`scripts/validate.sh`) verified PASS on clean state. All 5 synthetic violations correctly detected. Pre-commit hook runs gitleaks + validator in sequence; both must PASS. Two real validator bugs caught during E1 testing (octal leading-zero parsing, subshell variable isolation) and fixed.

2026-04-27 (MS-003) — DONE-003 signed. Three commits on `main`. SvelteKit scaffold runs clean (`npm run dev` = Vite 8.0.10, ready in 3.1s). Pre-commit hook live with gitleaks v8.30.1. All forms migrated under UnoAi name with role/descriptor carve-outs preserved.

## Engineer working agreements (running)

1. Verify RFI exists in source before writing `Closes: RFI-NNN` (INC-003).
2. Scope-by-section instructions cover all references, not just the named section (INC-004).
3. Capture pre-edit `wc -l` snapshots before multi-file MSes (DONE-002 lesson).
4. Don't authorise speculative prep work for Builder when "hold" is the answer.
5. Number sequentially from current state, no skip-numbering (Engineer error pattern, twice).
6. Repo-creation MSes verify remote state at create-time, not clone-time (INC-005).
7. Bash budgets are smell-checks not hard limits. When a budget overrun is caused by operator-required documentation or correctness fixes for bugs caught in test, the budget is wrong, not the script (DONE-004).
8. Tooling that enforces discipline must be tested against synthetic violations before shipping. Untested validators are worse than no validator — they create false confidence (DONE-004).
9. DONE sign-offs require mechanical verification of new tooling, not visual inspection. If a check was specced, the sign-off must include "ran the check against a synthetic violation, confirmed FAIL." Visual inspection of the script's presence is not sufficient (INC-006).
10. Bash budget caps for the validator are calibrated against the existing per-check complexity, not the new check's complexity. New checks that require new parsing primitives (block tracking, multi-file walks, structured parsing) will exceed the per-check budget by 2–3× and that's expected. Future budgets should be set at "current size + estimated new check size" rather than fixed caps (MS-005 RFI on validator size). **Numbering note:** operator wrote "#11" when introducing this; Builder renumbered to next-sequential **#10** per discipline #5. Operator can correct at DONE-005 sign-off if the skip was intentional.
11. Engineer prompts to Builder reflect committed file state, not chat-discussion state. References to prior "H1, H2, H2a" or similar inline-discussion labels are valid in chat but should not be replicated in prompts to Builder unless those labels also exist in committed files. When referencing prior decisions, cite the DEC/MS/INC number, not the chat-message annotation (DONE-005 sign-off).
12. When Engineer's external verification of repo state disagrees with Builder's report, the Engineer's tooling is the more likely failure point. HTML caches at GitHub, fetcher caches in the Engineer environment, and CDN edge caches all stale at multi-hour intervals. Builder running git locally against a known remote URL is the more reliable source of truth. When discrepancies appear, Engineer verifies via the API endpoint or `git ls-remote`, not by re-fetching HTML (DONE-006 sign-off, post-stale-cache incident).
13. Engineer pushback framing matters. "What state is the repo actually in?" is a neutral verification question. "Did Reaper's report land correctly?" is a question that implies fault before evidence. The first framing produces diagnostic responses; the second produces defensive ones. Future verification should default to neutral framing (DONE-006 sign-off).
14. **(refined 2026-04-28 at DONE-007 sign-off)** For DONE sign-off verification within ~60 seconds of a Builder push, prefer `git ls-remote origin refs/heads/main` (queries remote directly via git protocol, no HTTP cache layer). For verification beyond ~1 minute post-push, the API endpoint (`api.github.com/repos/.../git/refs/heads/main`) is sufficient. HTML repo pages should never be the verification source — they cache at multi-hour intervals. (Refinement followed the post-mortem on DONE-007 sign-off where API returned stale data within its 60s `max-age` cache window; original DONE-006-era wording defaulted to API-over-HTML, which was directionally correct but missed the API's own cache layer.)
15. Glossary, dictionary, and convention-list scopes that claim "cover all project terms" or "consistent across the repo" must be derived from grep/file-walk evidence, not from Engineer's memory of recent work. Memory-based coverage in a 7+ MS project produces ~80% coverage with the gaps in places Engineer hasn't recently touched (MS-008 post-mortem on glossary coverage gaps + en-GB premise that grep falsified).
16. Approval messages that contain placeholders for Engineer-drafted content must include the actual content inline, not "[paste from above]" references. Builder cannot resolve self-referential placeholders. If content exists in the chat thread but not in the approval message itself, Builder cannot use it. Engineer prompt-writing checklist item 5 (references resolve to committed files) extends to: "all content the prompt references must be present in the prompt itself." (MS-008 approval message contained two unfilled `[paste...]` placeholders that Builder caught + paused for clarification before applying.)
17. Engineer prompts to agent roles operating in different environments must not assume environment-specific paths. /mnt/skills/..., working folder paths, and other environment-bound references resolve in Engineer's chat session but not necessarily in Builder's terminal session, Inspector's terminal session, or future agent roles' sessions. Cross-environment references must be repo-relative or chat-pasted.
18. Each procedural step requires explicit written approval, even when prior approvals appear to imply it. Pre-conditions (access scope, doctrine load, dependency files) are not the same as the work approval itself. Builder/Inspector wait for the explicit "proceed" or "approved" instruction; Engineer must give it explicitly, not by implication.
19. Engineer must verify exact filesystem paths before naming them in agent prompts. "Where the repo lives" assumptions based on prior context are likely wrong; verify against the actual prior session's path before referencing it.
20. When introducing a new agent role (Inspector, future others), the role's procedural setup will reveal Engineer assumptions about access, paths, and tooling that don't survive contact with the agent's environment. Plan for the first session to surface 2-4 such friction points; treat them as expected, not as failures. The role becomes operational only after these are resolved.

## Banned moves (mirror of PROJECT.md, restated for session-start visibility)

The Builder never:

- Mutates a file without an approved METHOD_STATEMENT (granularity rule per DEC-012)
- Claims a task is done without proof attached to a DONE entry (proof-division per DEC-011)
- Adds a **direct top-level dependency after scaffolding** without a DECISION entry. (Framework template defaults from `npm create svelte` and similar are exempt — they're recorded once at scaffold time and not enumerated.)
- Expands scope without a CHANGE_ORDER
- Stores user conversation contents on any server
- Markets, designs, or copy-writes anything that targets minors
- Runs LLM calls through a proxy server we own (must be browser → vendor direct)
- Ships persona behaviour that makes permanence promises, claims to love the user, or presents itself as a substitute for human relationships. These are non-negotiable across all current and future shards (per DEC-015 + Appendix A).
- Commits any item from the **Sensitive content** list below, in any branch, ever. The pre-commit `gitleaks` hook (per DEC-024 + Procedure 8) is the enforcement layer. Bypassing it via `git commit --no-verify` is itself a banned move except as part of an INCIDENT response — and even then only after the rotated-secret + redacted-INCIDENT sequence in DEC-025 is followed.
- `Co-Authored-By Claude` footers on commits — operator-attributed commits only; AI-assistance attribution is a project policy decision filed as DEC if/when revisited.
